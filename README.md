# Candidate Bot - Автоматический мониторинг кандидатов

Автономный сервис, который мониторит Google Sheets на предмет новых кандидатов и отправляет напоминания в Telegram рекрутерам за день до выхода кандидата на работу.

## 🎯 Функционал

- ✅ Мониторинг Google Sheets на предмет новых кандидатов
- ✅ Автоматические напоминания в Telegram за день до выхода
- ✅ Сохранение истории в SQLite базе
- ✅ Поддержка разных форматов дат
- ✅ Запланированные проверки каждый час

## 📋 Требования к Google Sheets

Ваша таблица должна иметь следующие колонки (начиная с заголовков в 1-й строке):

| Колонка | Содержание | Пример |
|---------|-----------|--------|
| A | ФИО кандидата | Иван Петров |
| B | Объект/должность | Менеджер продаж |
| C | Дата выхода | 2026-01-20 или 20.01.2026 |
| D | ID рекрутера (опционально) | 123456789 |

## 🚀 Установка

### 1. Установить зависимости
```bash
pip install -r requirements.txt
```

### 2. Настроить Google Sheets API

#### Шаг 1: Создать проект в Google Cloud
1. Перейдите на https://console.cloud.google.com/
2. Создайте новый проект
3. Включите Google Sheets API
4. Создайте учётные данные (Service Account или OAuth 2.0)
5. Скачайте файл с ключами и переименуйте в `credentials.json`

#### Шаг 2: Получить ID таблицы
- Откройте вашу Google Sheets таблицу
- ID находится в URL: `https://docs.google.com/spreadsheets/d/{GOOGLE_SHEETS_ID}/edit`

### 3. Создать Telegram бота

#### Шаг 1: Создать бота
1. Напишите @BotFather в Telegram
2. Выполните `/newbot` и следуйте инструкциям
3. Скопируйте токен бота

#### Шаг 2: Получить ID чата рекрутера
1. Напишите боту любое сообщение
2. Откройте в браузере: `https://api.telegram.org/bot{YOUR_TOKEN}/getUpdates`
3. Найдите `chat.id` - это ID вашего чата

### 4. Настроить переменные окружения

Скопируйте файл `.env.example` в `.env` и заполните:

```bash
cp .env.example .env
```

Отредактируйте `.env`:
```
GOOGLE_SHEETS_ID=ваш_id_таблицы
GOOGLE_CREDENTIALS_FILE=credentials.json
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_RECRUITER_CHAT_ID=ваш_id_чата
CHECK_INTERVAL_HOURS=1
```

## 🎮 Запуск

```bash
python main.py
```

Вы увидите:
```
2026-01-19 10:30:45,123 - __main__ - INFO - 🚀 Запуск Candidate Bot...
2026-01-19 10:30:46,456 - __main__ - INFO - ✅ Бот подключён: @your_bot_name
2026-01-19 10:30:47,789 - __main__ - INFO - ⏰ Бот запущен. Проверка каждые 1 часа(ов)
```

## 📊 Структура проекта

```
candidate-bot/
├── main.py              # Главный файл с основной логикой
├── google_sheets.py     # Работа с Google Sheets API
├── telegram_bot.py      # Работа с Telegram Bot API
├── database.py          # Работа с SQLite базой
├── config.py            # Конфигурация
├── requirements.txt     # Зависимости
├── .env.example         # Пример переменных окружения
├── .env                 # Переменные окружения (не коммитить!)
├── credentials.json     # Ключи Google (не коммитить!)
├── token.json           # Токен OAuth (не коммитить!)
├── candidates.db        # SQLite база (не коммитить!)
└── README.md            # Этот файл
```

## 🔧 Настройка колонок в config.py

Если в вашей таблице другой порядок колонок, отредактируйте `config.py`:

```python
COLUMNS = {
    'name': 0,           # Номер колонки с ФИО (0=A, 1=B, 2=C...)
    'object': 1,         # Номер колонки с объектом
    'start_date': 2,     # Номер колонки с датой выхода
    'recruiter_id': 3    # Номер колонки с ID рекрутера
}
```

## ⏰ Настройка времени проверки

Измените `CHECK_INTERVAL_HOURS` в `.env`:
```
CHECK_INTERVAL_HOURS=1  # Проверка каждый час
CHECK_INTERVAL_HOURS=2  # Проверка каждые 2 часа
CHECK_INTERVAL_HOURS=24 # Проверка раз в день
```

## 🐛 Отладка

Все события логируются в консоль. Ищите:
- `🔍` - проверка кандидатов
- `✅` - успешные операции
- `❌` - ошибки
- `📱` - отправка сообщений в Telegram

## ⚠️ Важно

1. **Не коммитьте** `.env`, `credentials.json`, `token.json`, `candidates.db`
2. Добавьте эти файлы в `.gitignore`:
   ```
   .env
   credentials.json
   token.json
   candidates.db
   ```

3. Рекрутер должен предварительно написать боту в Telegram хотя бы одно сообщение

## 📝 Форматы дат

Поддерживаются следующие форматы:
- `2026-01-20` (YYYY-MM-DD)
- `20.01.2026` (DD.MM.YYYY)
- `20/01/2026` (DD/MM/YYYY)

## 🎓 Примеры использования

### Пример Google Sheets:
```
| ФИО              | Объект           | Дата выхода | ID рекрутера |
|------------------|------------------|-------------|--------------|
| Иван Петров      | Менеджер продаж  | 2026-01-20  | 123456789    |
| Мария Иванова    | Аналитик         | 2026-01-21  | 987654321    |
| Петр Сидоров     | Разработчик      | 2026-01-22  |              |
```

### Пример Telegram сообщения:
```
⚠️ Напоминание о выходе кандидата

Кандидат: Иван Петров
Объект: Менеджер продаж

🔔 Кандидат выходит на работу ЗАВТРА!
Пожалуйста, позвоните и уточните факт выхода.
```

## 💡 Советы

- Запустите бота на вашем сервере или используйте нгрок для локального тестирования
- Протестируйте с одним кандидатом перед полноценным развёртыванием
- Проверяйте логи при возникновении проблем

## 📞 Поддержка

При возникновении ошибок:
1. Проверьте файл `.env`
2. Убедитесь, что `credentials.json` скачан с Google Cloud
3. Проверьте токен бота и ID чата в Telegram
4. Посмотрите на логи в консоли
