# 🚀 Развертывание на Railway

Railway — это облачная платформа, которая идеально подходит для Python приложений. Ваш бот будет работать 24/7 независимо от вашего компьютера.

## Предварительные требования

1. **GitHub аккаунт** с вашим репозиторием (если его еще нет, создайте)
2. **Google Service Account ключ** (JSON файл)
3. **Telegram Bot Token** от @BotFather
4. **Railway аккаунт** (https://railway.app)

---

## Шаг 1: Подготовка GitHub репозитория

### 1.1 Инициализируем Git репо (если еще не сделано)
```bash
cd Candidate-bot
git init
git add .
git commit -m "Initial commit: Candidate Bot"
```

### 1.2 Создаем репозиторий на GitHub
1. Перейдите на https://github.com/new
2. Назовите репо `candidate-bot`
3. Нажмите **Create repository**

### 1.3 Загружаем код на GitHub
```bash
git remote add origin https://github.com/YOUR_USERNAME/candidate-bot.git
git branch -M main
git push -u origin main
```

> ⚠️ **Важно:** Убедитесь, что в `.gitignore` есть:
> ```
> .env
> credentials.json
> token.json
> candidates.db
> __pycache__/
> *.pyc
> ```

---

## Шаг 2: Создание Google Service Account

Railway не может запустить OAuth2 поток вручную, поэтому используем **Service Account**.

### 2.1 Создаем Service Account
1. Перейдите на https://console.cloud.google.com/
2. Выберите или создайте проект
3. Включите **Google Sheets API** (Search → Google Sheets API → Enable)
4. Слева: **Service Accounts** → **Create Service Account**
   - Name: `candidate-bot`
   - ID: `candidate-bot` (автозаполнится)
   - Нажмите **Create and Continue**

### 2.2 Даем права на Google Sheets
1. Жмите **Continue** в шаге 2
2. На шаге 3 (Grant users access) - пропустите (нажмите **Done**)

### 2.3 Создаем ключ
1. В списке Service Accounts найдите `candidate-bot`
2. Кликните на email адрес
3. Вкладка **Keys** → **Add Key** → **Create new key**
4. Выберите **JSON** → **Create**
5. JSON файл скачается - это ваш `credentials.json`

### 2.4 Даем доступ к Google Sheets
1. Откройте вашу таблицу: https://docs.google.com/spreadsheets/d/1jXV_w8PZ3cBAvJHYF5YgIph__O_5qXAxZW_rYOwvvvc
2. Нажмите **Share** (правый верхний угол)
3. Скопируйте email из скачанного JSON (выглядит как `...@...iam.gserviceaccount.com`)
4. Вставьте email в Share → **Share** с правом **Editor**

---

## Шаг 3: Получение Telegram Bot Token и Chat ID

### 3.1 Telegram Bot Token
1. В Telegram найдите **@BotFather**
2. Отправьте `/newbot`
3. Следуйте инструкциям, выберите имя
4. Скопируйте полученный токен (выглядит как `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

### 3.2 Telegram Chat ID (ваш ID)
1. Отправьте боту любое сообщение
2. Откройте в браузере:
   ```
   https://api.telegram.org/bot{YOUR_TOKEN}/getUpdates
   ```
   (вместо `{YOUR_TOKEN}` подставьте ваш токен)
3. Найдите в ответе `"chat":{"id":123456789}` - это ваш Chat ID

---

## Шаг 4: Развертывание на Railway

### 4.1 Регистрируемся на Railway
1. Перейдите на https://railway.app
2. Нажмите **Sign Up**
3. Авторизуйтесь через GitHub

### 4.2 Создаем новый проект
1. На Railway Dashboard: **New Project**
2. Выберите **Deploy from GitHub repo**
3. Нажмите **Configure GitHub App** (если нужно)
4. Найдите и выберите репо `candidate-bot`

### 4.3 Добавляем переменные окружения
После выбора репо Railway покажет форму переменных окружения.

**Добавьте эти переменные:**

| Ключ | Значение |
|------|---------|
| `GOOGLE_SHEETS_ID` | `1jXV_w8PZ3cBAvJHYF5YgIph__O_5qXAxZW_rYOwvvvc` |
| `TELEGRAM_BOT_TOKEN` | Ваш токен от BotFather |
| `TELEGRAM_RECRUITER_CHAT_ID` | Ваш Chat ID из Telegram |
| `GOOGLE_CREDENTIALS_FILE` | `credentials.json` |
| `CHECK_INTERVAL_HOURS` | `1` |

### 4.4 Добавляем Google Service Account JSON
1. В Railway Dashboard нажмите вкладку **Variables**
2. Нажмите **+ Add Variable**
3. Скопируйте **весь** содержимое скачанного JSON файла
4. В Railway создайте переменную:
   - **Key:** `GOOGLE_CREDENTIALS_JSON`
   - **Value:** Весь JSON (многострочный)

### 4.5 Обновляем код для работы с JSON из переменных окружения

Отредактируйте `google_sheets.py`, чтобы поддерживать JSON из переменных:

```python
# В начало файла добавьте:
import json

# В методе _get_service() добавьте перед проверкой credentials:
creds_json = os.getenv('GOOGLE_CREDENTIALS_JSON')
if creds_json:
    # Используем JSON из переменной окружения
    import io
    creds = Credentials.from_service_account_info(
        json.loads(creds_json), 
        scopes=SCOPES
    )
    return build('sheets', 'v4', credentials=creds)
```

### 4.6 Нажимаем Deploy
1. Нажмите **Deploy** на Railway
2. Смотрите логи в реальном времени
3. Если видите `⏰ Бот запущен. Проверка каждые 1 часа(ов)` — успешно!

---

## Шаг 5: Проверка работы

### Логи
1. В Railway Dashboard откройте **Logs**
2. Должны видеть примерно так:
   ```
   🚀 Запуск Candidate Bot...
   ✅ Бот подключён: @your_bot_name
   ⏰ Бот запущен. Проверка каждые 1 часа(ов)
   🔍 Проверка кандидатов в Google Sheets...
   ```

### Тест сообщения
Если все работает, завтра бот отправит вам напоминание через Telegram о кандидатах, у которых день выхода.

---

## Шаг 6: Пересоздание базы данных на Railway

**Важно:** Railway использует эфемерный файловой системе (данные удаляются при рестарте).

Добавим **KV хранилище** вместо SQLite:

1. В Railway Dashboard: **Plugins** → **Add Plugin** → **PostgreSQL** или **Redis**
2. Обновим `database.py` для использования облачной БД

Или используем более простой вариант — **файловое хранилище** в `/tmp`, но оно теряется при рестартах.

**Рекомендация:** Используйте PostgreSQL (добавляется бесплатно в Railway):

```bash
pip install psycopg2-binary
```

И обновите connection в `database.py`:
```python
import psycopg2
import os

DATABASE_URL = os.getenv('DATABASE_URL')
conn = psycopg2.connect(DATABASE_URL)
```

---

## Устранение неполадок

### Бот не запускается
- Проверьте **Logs** в Railway
- Убедитесь, что все переменные окружения установлены
- Проверьте синтаксис `GOOGLE_CREDENTIALS_JSON`

### Напоминания не отправляются
- Проверьте, что `TELEGRAM_BOT_TOKEN` и `TELEGRAM_RECRUITER_CHAT_ID` верные
- Убедитесь, что в таблице есть кандидаты с датой выхода завтра
- Посмотрите логи в Railway

### "Access denied" для Google Sheets
- Убедитесь, что вы поделились таблицей с email сервисного аккаунта
- Проверьте что Service Account имеет права **Editor**

---

## Остановка бота

Если нужно остановить бота:
1. Railway Dashboard → **Settings** → **Environment**
2. Удалите все переменные или нажмите **Pause Project**
3. Проект приостановится, но данные сохранятся

---

## Стоимость

**Railway бесплатный план:**
- ✅ $5/месяц бесплатный кредит
- ✅ До 500 часов computations в месяц
- ✅ Бот работает ~730 часов/месяц... **переходит на платный план за ~$5-10/месяц**

Если нужен полностью бесплатный вариант — рассмотрите **Render.com** (12 часов free tier, но есть лучше через cron).

---

**Готово! Ваш бот теперь работает 24/7 на Railway!** 🚀
